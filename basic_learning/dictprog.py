dict = {"mulatable":"which can change", "immutable":"which can't get change", "enormous":"something which is huge", "cappin":"showing off something"}

var = input("please enter a word to get it's meaning\n")

print(dict[var])